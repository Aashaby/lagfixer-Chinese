AbilityLimiter:
  enabled: true
  worlds: # 使用 '*' 对所有世界启用。
    - '*'
  values:
    elytra_boost:
      cooldown: 5
      # 该选项已被开发者禁用
      speed_multiplier: 0.85
      # 设为 0 可禁用
      additional_durability_loss: 5

    trident_riptide:
      cooldown: 3
      # 该选项已被开发者禁用
      speed_multiplier: 0.85
      # 设为 0 可禁用
      additional_durability_loss: 5