# 应用更改需要重启服务器。
# 通过将碰撞替换为新的优化对象来提升性能。
VehicleMotionReducer:
  enabled: false
  worlds: # 使用 '*' 对所有世界启用。
    - '*'
  values:
    # 实验性功能
    force_load: false

    minecart:
      enabled: true
      # 移除自然生成的矿车，适用于频繁生成新区块的服务器。
      remove_chest: false

    boat:
      enabled: true